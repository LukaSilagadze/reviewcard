# ReviewCard

Static bilingual marketing website built with React and Vite.

```bash
npm install
npm run dev
```

Editable content lives in `src/i18n`, `src/data`, and `src/config`. Replaceable images live in `public/assets`.
