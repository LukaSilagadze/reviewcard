export const socials = { instagram: '#', facebook: '#', tiktok: '#', whatsapp: '#' };
export const partners = Array.from({ length: 7 }, (_, i) => i + 1);
export const testimonials = Array.from({ length: 5 }, (_, i) => i + 1);
