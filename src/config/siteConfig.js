export const socials = { instagram: '#', facebook: '#', tiktok: '#', whatsapp: '#' };
export const contact = { phone: '+995 555 00 00 00', email: 'info@reviewcard.ge' };
export const partners = Array.from({ length: 7 }, (_, i) => i + 1);
export const testimonials = Array.from({ length: 5 }, (_, i) => i + 1);
export const customerGallery = Array.from({ length: 6 }, (_, i) => `/assets/customer-gallery/customer-${i + 1}.svg`);
